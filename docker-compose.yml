version: '3.8'

services:
  # Redis - 潮汐记忆缓存
  redis:
    image: redis:7-alpine
    container_name: miya-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 3
    restart: unless-stopped
    networks:
      - miya-network

  # Etcd - Milvus依赖
  etcd:
    image: quay.io/coreos/etcd:v3.5.5
    container_name: miya-etcd
    environment:
      - ETCD_AUTO_COMPACTION_MODE=revision
      - ETCD_AUTO_COMPACTION_RETENTION=1000
      - ETCD_QUOTA_BACKEND_BYTES=4294967296
      - ETCD_SNAPSHOT_COUNT=50000
    volumes:
      - etcd_data:/etcd
    command: etcd -advertise-client-urls=http://127.0.0.1:2379 -listen-client-urls http://0.0.0.0:2379 --data-dir /etcd
    healthcheck:
      test: ["CMD", "etcdctl", "endpoint", "health"]
      interval: 10s
      timeout: 3s
      retries: 3
    restart: unless-stopped
    networks:
      - miya-network

  # MinIO - Milvus对象存储
  minio:
    image: minio/minio:RELEASE.2023-03-20T20-16-18Z
    container_name: miya-minio
    environment:
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
    ports:
      - "9001:9001"
      - "9000:9000"
    volumes:
      - minio_data:/minio_data
    command: minio server /minio_data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 3s
      retries: 3
    restart: unless-stopped
    networks:
      - miya-network

  # Milvus - 向量数据库
  milvus:
    image: milvusdb/milvus:v2.4.0
    container_name: miya-milvus
    command: ["milvus", "run", "standalone"]
    environment:
      ETCD_ENDPOINTS: etcd:2379
      MINIO_ADDRESS: minio:9000
    volumes:
      - milvus_data:/var/lib/milvus
    ports:
      - "19530:19530"
      - "9091:9091"
    depends_on:
      - "etcd"
      - "minio"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9091/healthz"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 40s
    restart: unless-stopped
    networks:
      - miya-network

  # Neo4j - 知识图谱
  neo4j:
    image: neo4j:5.20
    container_name: miya-neo4j
    environment:
      - NEO4J_AUTH=neo4j/miya_password_2026
      - NEO4J_dbms_memory_pagecache_size=512m
      - NEO4J_dbms_memory_heap_initial__size=512m
      - NEO4J_dbms_memory_heap_max__size=512m
    volumes:
      - neo4j_data:/data
      - neo4j_logs:/logs
      - neo4j_plugins:/plugins
    ports:
      - "7474:7474"  # HTTP
      - "7687:7687"  # Bolt
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:7474 || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 30s
    restart: unless-stopped
    networks:
      - miya-network

  # 可选：Redis Commander - Redis可视化管理工具
  redis-commander:
    image: rediscommander/redis-commander:latest
    container_name: miya-redis-commander
    environment:
      - REDIS_HOSTS=local:redis:6379
    ports:
      - "8081:8081"
    depends_on:
      - redis
    restart: unless-stopped
    networks:
      - miya-network
    profiles:
      - tools

  # 可选：Neo4j Bloom - Neo4j可视化工具
  # 注意：需要Neo4j Enterprise许可证
  # neo4j-bloom:
  #   image: neo4j-bloom:5.20
  #   container_name: miya-neo4j-bloom
  #   environment:
  #     - NEO4J_URI=bolt://neo4j:7687
  #     - NEO4J_AUTH=neo4j/miya_password_2026
  #   ports:
  #     - "7473:7473"
  #   depends_on:
  #     - neo4j
  #   restart: unless-stopped
  #   networks:
  #     - miya-network
  #   profiles:
  #     - tools

volumes:
  redis_data:
    driver: local
  etcd_data:
    driver: local
  minio_data:
    driver: local
  milvus_data:
    driver: local
  neo4j_data:
    driver: local
  neo4j_logs:
    driver: local
  neo4j_plugins:
    driver: local

networks:
  miya-network:
    driver: bridge
