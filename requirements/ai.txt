# ========================================
# Miya - AI 模型依赖
# ========================================
# 说明：AI 功能相关依赖
# 包含 OpenAI 兼容 API、嵌入模型等
# 安装命令: pip install -r requirements/ai.txt
# ========================================

# ========== OpenAI 兼容 API ==========
# OpenAI 官方客户端 (支持 DeepSeek、通义千问等兼容 API)
openai>=1.3.0,<2.0.0

# ========== 其他 AI 提供商 ==========
# Claude API 客户端 (Anthropic)
anthropic>=0.7.0,<1.0.0
# 智谱 AI (GLM) 客户端
zhipuai>=4.0.0,<5.0.0

# ========== 本地嵌入模型 ==========
# Sentence Transformers (用于本地向量嵌入)
sentence-transformers>=2.2.0,<3.0.0

# ========== 深度学习框架 ==========
# PyTorch (CPU 版本，如需 GPU 版本请根据硬件选择)
# CPU 版本
torch>=2.0.0,<2.3.0
# 注意：如需 GPU 加速，请根据 CUDA 版本选择：
# CUDA 11.8: pip install torch --index-url https://download.pytorch.org/whl/cu118
# CUDA 12.1: pip install torch --index-url https://download.pytorch.org/whl/cu121

# ========== 文本处理 ==========
# Hugging Face Transformers
transformers>=4.30.0,<5.0.0
